/// <reference path="../node_modules/@types/p5/global.d.ts" />

function setup() {
  noLoop();
  pixelDensity(1);
  const c = createCanvas(1080, 1350);
  commonSetup(c, 'Fidenza');
}

function draw() {
}
